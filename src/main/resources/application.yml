spring:
  datasource:
    url: jdbc:h2:file:/Users/nikit/desktop/Uni/3. Semester/Webtech/Projekt/database
    username: nikit
    password: password
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
      settings.trace: false
      settings.web-allow-others: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        globally_quoted_identifiers: true
  sql:
    init:
      platform: h2
      mode: always